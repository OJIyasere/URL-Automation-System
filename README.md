# Week 8, Mid Fidelity Prototype

1. Please review the UML diagram for the mid fidelity prototype

2. Read the header comments on the python files to see what each file does

3. Write code implementing the intended function of your file

When all the parts are done, a user can download all files in midFidelityPrototype, run command "python GUI_Broken_Link_Checker.py" to launch the program, and that program should work as intended for the prototype (may need pip to install additional modules like tkinter, beautifulsoup, and csv)

### When updating the github with your code, please run:

git branch (this tells you what branch youre working on)

"git add file" (DO NOT "git add --all")

git commit -m "Informative commit message"
